import {createTheme} from "@mui/material"

const theme = createTheme({
    palette: {

    },
    breakpoints: {
        values: {
            xs: 0,
            sm: 600,
            md: 960,
            lg: 1024,
            xl: 1280,
        },
    },
});

export default theme
